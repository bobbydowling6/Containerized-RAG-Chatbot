# Containerized-RAG-Chatbot
This project builds and containerizes a minimal RAG-powered chatbot that runs entirely with Miniforge3 compose up that's related to docker. The chatbot ingests documents, stores them in ChromaDB, retrieves relevant chunks for user questions, generates grounded answers via Google Gemini, and presents everything through a Streamlit interface.
